# 智能推荐 Skill 移入聊天框 + 号菜单总结 (Walkthrough)

## 1. 核心改进清单

### 1.1 移除主页独立条目，保持主视觉绝对清爽
- 从主面板中彻底移除了文件列表与输入框之间的独立推荐横幅条 `smartSkillRecommendationSection`；
- 文件列表与输入框无缝贴合衔接，释放更多文件展示区域。

### 1.2 聊天输入框内嵌 `+` 号 Skill 呼出菜单
- **输入框左侧常驻 `+` 号按钮**：
  - 点击呼出精简菜单，**首项置顶展示「✨ 智能推荐」**（实时结合所选文件的扩展名动态感知匹配）；
  - 分组展示 **🧩 常用文件 Skill**（尺寸调整、格式转换、文档转PDF、PDF合并拆分、批量重命名、EXIF清理）；
  - 分组展示 **🏢 企业协同 Skill**（飞书、企业微信、钉钉）；
  - 底部提供 **⚙️ 打开 Skill 管理中心...** 的快捷跳转；
- **一键填入**：点击任意推荐或常用 Skill，对应动作指令即刻填入输入框，由用户确认后回车发送。

---

## 2. 自动化测试

- 全量 **31 个单元测试全部通过（100% Pass, 0 failures）**。
