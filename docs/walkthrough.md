# 模型配置去除冗余控件总结 (Walkthrough)

## 1. 核心改进清单

### 1.1 本地 CLI 引擎 (`.cliModel`)
- **彻底去除并列输入框**：已检测到可用模型的 CLI（如 `agy` 原生内置 `gemini-3.5-flash`、`gemini-3.7-flash` 等），直接呈现单一、干净的 **下拉选择器 (`Picker`)**，不再并列展示同名文本框；
- 无预设模型列表的自定义 CLI 保持呈现单一输入框。

### 1.2 云端 API 引擎 (`.cloudModel`)
- **统合预设推荐模型与模型输入**：将原先拆开的「预设推荐模型」与「模型名称 (可编辑)」统合为单一的 **「选用模型」**；
- 预设服务商（DeepSeek、OpenAI 等）直接下拉选择，消除重复行。

---

## 2. 自动化测试

- 全量 **31 个单元测试全部通过（100% Pass, 0 failures）**。
