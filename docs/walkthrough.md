# 导航重构与系统偏好增强总结 (Walkthrough)

## 1. 核心改进清单

### 1.1 导航分组与文案精确调整
- **AI 引擎模式 (二选一)**：
  - 🌐 **云端 API 引擎** (`.cloudModel`)
  - 💻 **本地 CLI 引擎** (`.cliModel`)
- **功能扩展** (原"功能扩展与系统"已精准拆分)：
  - 🧩 **本地技能库** (`.skills`)
  - ☁️ **云端技能库** (`.marketplace`)
- **系统设置** (提升为独立顶级导航分组)：
  - ⚙️ **偏好与系统** (`.general`)

### 1.2 系统偏好页能力扩充与前瞻设计
- **开机自动启动 (Launch at Login)**：
  - 支持一键开关开机自启，集成 macOS 原生 `SMAppService.mainApp` 后台常驻能力；
- **窗口交互呈现模式 (预留 Mini 聊天框)**：
  - 🪟 **标准面板模式 (默认)**：完整文件树状与平铺列表、任务看板与完整输入面板；
  - 💬 **Mini 聊天框模式**：极简悬浮输入胶囊与即时触发模式，为后续精简悬浮窗交互打好架构基础；
- **全局快捷键与单进程安全**：
  - 规范展示 `⌥ M` 呼出/隐藏、`⌘ Z` 撤销、`⌘ Q` 退出，实时监控 POSIX 独占锁与原子事务回滚栈。

---

## 2. 自动化测试

- 全量 **31 个单元测试全部通过（100% Pass, 0 failures）**。
